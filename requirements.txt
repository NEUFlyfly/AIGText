# Python 依赖清单
# 安装: pip install -r requirements.txt
#
# --- 核心 (已内置) ---
# (标准库, 无需安装)
#
# --- 测试 ---
pytest>=8.0

# --- RAG 检索增强 ---
chromadb>=1.5                    # 向量数据库
sentence-transformers>=5.0       # embedding 模型
#
# --- 电脑A: 语言服务 ---
# flask              # HTTP 服务框架 (轻量, 适合本地部署)
#
# --- 电脑B: 视觉服务 ---
# torch              # PyTorch (视觉模型推理)
# torchvision        # 图像预处理 + 预训练模型
pillow>=10.0                     # 图像读取
#
# === 可选 ===
# opencv-python      # 更强大的图像处理 (如果 torchvision 不够用)
# tiktoken           # token 计数 (精确控制 RAG context 大小)
